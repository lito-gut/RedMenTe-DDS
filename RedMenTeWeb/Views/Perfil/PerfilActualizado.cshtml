@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5 text-center">
    <h2 class="text-success">¡Perfil actualizado exitosamente!</h2>
    <p>Los cambios realizados se han guardado correctamente.</p>
    <a href="@Url.Action("Index", "Perfil")" class="btn btn-primary mt-3">Volver al perfil</a>
</div>
